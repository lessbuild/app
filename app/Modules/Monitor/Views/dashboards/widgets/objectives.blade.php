<x-signal.ui.panel as="section" class="min-w-0 p-5">
    <div class="flex items-start justify-between gap-3"><div><p class="text-xs font-bold uppercase tracking-[0.16em] text-warning dark:text-warning">RELIABILITY</p><h2 class="mt-2 text-lg font-bold">SLO health</h2></div><a href="{{ route('monitor.objectives.index') }}" class="text-xs font-bold text-primary hover:underline dark:text-primary">View SLOs →</a></div>
    <div class="mt-5 space-y-3">@forelse($data['objectives'] as $item)@php($report = $item['report'])<x-signal.ui.card as="a" href="{{ route('monitor.objectives.show', $item['objective']) }}" class="shadow-none block p-4 hover:border-primary dark:hover:border-primary"><div class="flex items-center justify-between gap-3"><span class="truncate text-sm font-semibold">{{ $item['objective']->name }}</span><x-monitor::ui.badge :tone="$report['status'] === 'healthy' ? 'green' : ($report['status'] === 'warning' ? 'amber' : 'red')">{{ $report['compliance'] === null ? 'No data' : number_format($report['compliance'], 2).'%' }}</x-monitor::ui.badge></div><p class="mt-2 text-xs text-muted dark:text-subtle">{{ $item['objective']->indicatorLabel() }} · {{ $item['objective']->scopeLabel() }} · {{ $report['budget_remaining'] === null ? 'Budget not calculated' : number_format($report['budget_remaining'], 1).'% budget remaining' }}</p></x-signal.ui.card>@empty<p class="py-5 text-sm text-muted dark:text-subtle">No enabled SLOs configured.</p>@endforelse</div>
</x-signal.ui.panel>
