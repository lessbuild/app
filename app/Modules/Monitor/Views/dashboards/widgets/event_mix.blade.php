<section class="ui-panel min-w-0 p-5">
    <div class="flex items-start justify-between gap-3"><div><p class="text-xs font-bold uppercase tracking-[0.16em] text-info dark:text-info">TELEMETRY</p><h2 class="mt-2 text-lg font-bold">Event mix</h2></div><x-monitor::ui.badge tone="sky">{{ number_format($data['eventCount']) }} total</x-monitor::ui.badge></div>
    <dl class="mt-6 space-y-4">@foreach(['request' => 'Requests', 'query' => 'Queries', 'job' => 'Jobs', 'exception' => 'Exceptions', 'log' => 'Logs', 'metric' => 'Metrics', 'other' => 'Other'] as $type => $label)<div><div class="flex items-center justify-between gap-3 text-xs"><dt class="font-semibold">{{ $label }}</dt><dd>{{ number_format($data['breakdown'][$type] ?? 0) }}</dd></div><div class="mt-2 h-2 rounded-full bg-surface-muted dark:bg-surface-muted"><div class="h-2 rounded-full bg-info" style="width: {{ $data['eventCount'] > 0 ? min(100, ($data['breakdown'][$type] ?? 0) / $data['eventCount'] * 100) : 0 }}%"></div></div></div>@endforeach</dl>
</section>
