<section class="ui-panel min-w-0 p-5">
    <div class="flex items-start justify-between gap-3"><div><p class="text-xs font-bold uppercase tracking-[0.16em] text-success dark:text-success">UPTIME</p><h2 class="mt-2 text-lg font-bold">Monitor health</h2></div><a href="{{ route('monitor.monitors.index') }}" class="text-xs font-bold text-primary hover:underline dark:text-primary">View monitors →</a></div>
    <div class="mt-5 grid gap-3 sm:grid-cols-2">@forelse($data['monitors'] as $monitor)<a href="{{ route('monitor.monitors.show', $monitor) }}" class="ui-card shadow-none p-4 hover:border-primary dark:hover:border-primary"><div class="flex items-center justify-between gap-2"><p class="truncate text-sm font-semibold">{{ $monitor->name }}</p><x-monitor::ui.badge :tone="$monitor->healthLabel() === 'Up' ? 'green' : ($monitor->healthLabel() === 'Down' ? 'red' : 'amber')">{{ $monitor->healthLabel() }}</x-monitor::ui.badge></div><p class="mt-2 truncate text-xs text-muted dark:text-subtle">{{ $monitor->typeLabel() }} · {{ $monitor->environment->application->name }}</p></a>@empty<p class="py-5 text-sm text-muted dark:text-subtle">No monitors configured.</p>@endforelse</div>
</section>
