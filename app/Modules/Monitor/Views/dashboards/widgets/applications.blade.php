<x-signal.ui.panel as="section" class="min-w-0 p-5">
    <div class="flex items-start justify-between gap-3"><div><p class="text-xs font-bold uppercase tracking-[0.16em] text-primary dark:text-primary">INVENTORY</p><h2 class="mt-2 text-lg font-bold">Applications</h2></div><a href="{{ route('monitor.applications.index') }}" class="text-xs font-bold text-primary hover:underline dark:text-primary">View applications →</a></div>
    <div class="mt-5 space-y-3">@forelse($data['applications'] as $application)<x-signal.ui.card as="a" href="{{ route('monitor.applications.show', $application) }}" class="shadow-none flex items-center justify-between gap-3 p-4 hover:border-primary dark:hover:border-primary"><span class="min-w-0"><span class="block truncate text-sm font-semibold">{{ $application->name }}</span><span class="mt-1 block text-xs text-muted dark:text-subtle">{{ $application->framework ?: 'Other' }} · {{ $application->environments_count }} environments</span></span><span class="shrink-0 text-right text-xs text-muted dark:text-subtle">{{ $application->last_receipt_at?->utc()->format('M j, H:i') ?? 'No data' }}</span></x-signal.ui.card>@empty<p class="py-5 text-sm text-muted dark:text-subtle">No applications configured.</p>@endforelse</div>
</x-signal.ui.panel>
