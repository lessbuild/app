# Analytics module source

Imported from `https://github.com/lessbuild/analytics` at commit `d762d0286bc481423994c29ffe432fd1c4e16209` (2026-09-23 source audit).

Routes and migrations are namespaced to the Analytics module and use the named `analytics` database connection. The module remains disabled until shared Core authentication, workspace/project authorization, product billing, and migration/regression evidence are ready. Do not enable this module as a standalone local-auth application.
