name: Module boundaries

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check product module dependencies
        run: php scripts/check-module-boundaries.php
