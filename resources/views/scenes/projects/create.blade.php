<x-layouts.app>
    <x-layouts.partials.breadcrumbs :route="route('projects.index')" :title="__('Back to applications')" />
    <form method="POST" action="{{ route('projects.store') }}" class="mt-6">@csrf
        <x-forms.section :title="__('New application')" :description="__('Start from a production-ready template, then customize every runtime setting.')"><div class="space-y-5 bg-primary p-6"><label class="block"><span class="mb-1 block text-sm text-secondary">{{ __('Name') }}</span><input required name="name" value="{{ old('name') }}" class="input secondary rounded"></label><x-forms.errors name="name"/><label class="block"><span class="mb-1 block text-sm text-secondary">{{ __('Description') }}</span><textarea name="description" rows="4" class="input secondary rounded">{{ old('description') }}</textarea></label><x-forms.errors name="description"/><fieldset><legend class="mb-2 text-sm font-bold text-primary">{{ __('Application template') }}</legend><div class="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">@foreach($templates as $value=>$template)<label class="flex cursor-pointer gap-3 rounded-xl border border-primary bg-secondary p-4"><input type="radio" name="preset" value="{{ $value }}" @checked(old('preset','laravel')===$value)><span><strong class="block text-primary">{{ $template['name'] }}</strong><span class="mt-1 block text-xs leading-5 text-secondary">{{ $template['description'] }}</span><span class="mt-2 inline-block rounded-full bg-primary px-2 py-0.5 text-[10px] font-bold uppercase text-secondary">{{ $template['runtime_type'] }}</span></span></label>@endforeach</div></fieldset><x-forms.errors name="preset"/></div><x-slot:footer><div class="bg-tertiary px-6 py-4 text-right"><button type="submit" class="button primary">{{ __('Create application') }}</button></div></x-slot:footer></x-forms.section>
    </form>
</x-layouts.app>
