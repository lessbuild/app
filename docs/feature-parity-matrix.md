# Unified platform feature-parity matrix

Prepared 23 September 2026 against the supplied source revisions below. This is a capability-level inventory from routes, controllers, models, migrations, jobs, commands, notifications, and configuration. It is a traceability checklist, not proof of behavioral parity: Monitor and Analytics source lives in peer modules, eligible production records have been imported into separate product databases and Core, and workflow-by-workflow evidence still needs to be recorded before cutover.

| Product | Source repository | Audited source revision | Initial route definitions | Migrations |
| --- | --- | --- | ---: | ---: |
| Deployer | [lessbuild/app](https://github.com/lessbuild/app) | `210b7d1ffcd1c6b565383ae0213c414e84c673cf` | 306 web/API definitions | 134 |
| Monitor | [lessbuild/monitor](https://github.com/lessbuild/monitor) | `c5b3d9a4870b5dc296837228c3209d946c7d0b4e` | 137 web/API definitions | 43 |
| Analytics | [lessbuild/analytics](https://github.com/lessbuild/analytics) | `d762d0286bc481423994c29ffe432fd1c4e16209` | 29 web/API definitions | 20 |

The counts describe the audited baseline and were produced from route declarations, so they are not unique customer features. Deployer routes, HTTP code, models, actions, jobs, notifications, commands, policies, services, scripts, product views, and migrations now reside under `app/Modules/Deployer`; `database/migrations` and the prior product-view paths remain compatibility symlinks. Shared Signal layouts and UI components remain in `resources/views`. Monitor and Analytics are imported under their modules. Their code defaults remain disabled until integration readiness, while the current production environment overrides both to enabled with Core authentication; that runtime setting is not evidence that their full feature parity is complete. Matrix rows record tested slices separately from remaining behavioral, role, data, and production-engine gates.

## Deployer

Source paths below are relative to the Deployer repository at the audited revision. Existing organization-owner subscription behavior must become a workspace-owned Deployer subscription while keeping plans, quotas, renewal state, and customer entitlements intact.

| ID | Capability to preserve | Source evidence to audit/migrate | Unified destination | Data, access, and integration constraints | Status / regression evidence |
| --- | --- | --- | --- | --- | --- |
| D01 | Registration, sign-in/out, email verification, password reset, social login, two-factor challenges, recovery codes, password confirmation, and session management | `app/Http/Controllers/Auth/*`, `app/Http/Controllers/UsersController.php`, `app/Http/Controllers/TwoFactorAuthenticationController.php`; `routes/auth.php` | `app/Core/Auth` | Preserve account IDs, verified state, password hashes, social identities, MFA/recovery state, throttles, and security policy; do not merge accounts by email alone. | Deployer account/security flows remain in the module. Production import applied eligible credentials, social identities, and re-encrypted MFA values without merging by email; the requested Core account is verified and has reconciled Deployer and Analytics identity maps, while its Monitor source identity remains held for review. Core password login, registration, signed email verification, and host-only session handoff are implemented, but full product security regression and cross-host acceptance remain open. |
| D02 | Organizations, members, roles, invitations, switching, notification/security policy, SSO, and organization deletion | `OrganizationController`, `EnterpriseSsoController`, organization policies/models and migrations; organization routes | Core workspaces/memberships/invitations plus Deployer-specific access projection | Preserve membership/role semantics, invitation expiry, owner safeguards, SSO restrictions, audit trail, and organization-owned resources. | Core provides Signal-based owner/admin invitation creation, email-bound acceptance, and revocation with hashed expiring tokens. Workspace owners can change non-owner roles; owners/admins can revoke eligible members, which revokes Core product grants and project memberships and writes a workspace audit event that owners/admins can review on the team page. Deployer invitation data is importer-mapped but customer-data reconciliation, local product-principal provisioning, legacy role semantics, Deployer SSO restrictions, and cross-product authorization evidence remain open. |
| D03 | Deployer plan catalog, checkout, billing portal, cancellation/resume, seat metering, and quotas | `BillingController`, Cashier/subscription models, `SyncOrganizationSeatQuantityJob`, billing routes/config | `app/Modules/Deployer/Billing` with Core workspace billing owner | Keep a distinct Deployer subscription per workspace; migrate customer/payment/subscription IDs and current entitlement state; changing Monitor/Analytics plans must not affect it. | The Core importer has been applied to production after backup. One unbilled Deployer workspace slot is reconciled; the single existing paid owner subscription remains in the source database and two organization slots are held because that subscription is shared by their owner. A typed Core resolver and entitlement/quota adapter remain opt-in (`DEPLOYER_PLAN_AUTHORITY=core`). Focused tests verify snapshot-based grants/limits and fail-closed reconciliation. Multi-organization billing ownership, checkout/portal/webhook integration, seat-metering regression, and broader entitlement/quota evidence remain open. |
| D04 | Projects, environments, preview settings, variables/secrets, processes, resources, deployment controls, and environment runtimes | `ProjectController`, `EnvironmentController`, related models/policies and migrations; project/environment routes | Core canonical project mapping plus `app/Modules/Deployer/Projects` | Preserve project/environment identifiers through mapping, secret encryption, scoping, production controls, and historical resource ownership. Never infer an environment mapping across products. | Deployer project/environment flows remain in the module. Core can explicitly link authorized existing Deployer projects and local environments to a canonical project, requiring a same-project canonical environment for each linked local environment. Authorized links now carry a request-local Deployer organization context and recheck local membership plus the Core product grant without changing the saved workspace selection. The environment settings dialog now composes shared Signal input, select, checkbox, and button components with unique IDs, accessible validation feedback, and preserved old-input/unchecked semantics; Deployer, Core, Analytics, browser, and full-suite regression checks pass. The deployment-controls form, source-data import reconciliation, full action/job context propagation, and broader regression evidence remain open. |
| D05 | Configuration review, compare/observe, plan/apply, operation cancellation/retry, receipts, and control-plane configuration APIs | `ApplicationConfigurationController`, configuration actions/models/migrations, `Api/V1/ControlPlaneController` | `app/Modules/Deployer/Configuration` and Deployer API | Keep plans non-mutating; retain approval boundaries, operation idempotency, encrypted data, actor/audit records, and machine-token abilities. | Configuration behavior remains in the module; operation lifecycle, API regression, and Core access evidence are open. |
| D06 | GitHub App setup/connect/callback, repository discovery, GitHub webhooks, provider connections, repository management, deployment filters, and webhook history/retry | `GitHubAppController`, `GitHubAppSetupController`, `GitHubAppWebhookController`, `RepositoriesController`, repository webhook controllers/models/jobs | `app/Modules/Deployer/Repositories` and `Integrations` | Retain OAuth/App installations, webhook signature checks/deduplication, repository identity, permissions, delivery records, and safe retry semantics. | Provider and webhook behavior remains in the module; callback, signature, deduplication, and retry evidence is open. |
| D07 | Builds, build logs, comparisons, cancel/retry, release metadata, acceptance audit, promotion approval, promotion lineage, rollback, and deployment history | `BuildsController`, build callbacks, `BuildPromotionController`, `BuildRevisionCallbackController`, build/repository jobs and notifications | `app/Modules/Deployer/Deployments` | Preserve state transitions, commit/revision identity, approvals, immutable history, log access, release lineage, and rollback safety. | Deployment behavior remains in the module; lifecycle, approval, rollback, and regression evidence is open. |
| D08 | Preview deployments, per-preview secret approval, quotas, cleanup/retry, GitHub preview reports, and preview expiry | Project preview routes, preview models/migrations, `CleanupPreviewStackJob`, `ReportGitHubPreviewJob`, `ExpirePreviewDeploymentsCommand` | `app/Modules/Deployer/Previews` | Keep quota locking, secret approval, repository/environment scope, cleanup evidence, and safe idempotent retry. | Preview behavior remains in the module; quota, race, and cleanup regression evidence is open. |
| D09 | Server provisioning, initialization, provider import/review/confirmation, server catalog, provider health and connection checks | `ServersController`, `ImportServerController`, `ProvidersController`, `ProviderServerCatalogController`, server/provider jobs and commands | `app/Modules/Deployer/Servers` and `Providers` | Preserve provider credentials encrypted at rest, imported-server assessment and confirmation, ownership, provisioning stages, and external API protections. | Provider/import/provisioning behavior remains in the module; provider and provisioning regression evidence is open. |
| D10 | Server commands, command history/reruns, logs, metrics, diagnostics, troubleshooting broker/sessions/frames, SSH-key security, and server deletion | `ServerCommandsController`, `CommandsController`, `ServerTroubleshootingController`, server jobs/commands, server models/migrations | `app/Modules/Deployer/Servers/Operations` | Keep command authorization, audit/retention, SSH secrecy, broker isolation, remote output redaction, and session expiry. | Privileged server-operation behavior remains in the module; authorization, redaction, and retention evidence is open. |
| D11 | Website create/import/deploy/remove, placement cleanup, process/runtime setup, scoped environment configuration, and website deployment callbacks | `WebsitesController`, `ImportWebsiteController`, website callbacks, `app/Jobs/Web/*`, website models/migrations | `app/Modules/Deployer/Websites` | Preserve deployment state, server placement, encrypted environment values, callback authentication, and cleanup recovery. | Website behavior remains in the module; create/import/deploy/cleanup regression evidence is open. |
| D12 | Website domains, temporary domains, DNS synchronization, certificate checks, Caddy configuration, and domain callbacks | `DomainController`, `ApplyWebsiteDomainsJob`, `AddWebsiteToCaddyCommand`, `CheckDomainCertificatesCommand`, domain migrations | `app/Modules/Deployer/Domains` | Preserve domain ownership/verification, TLS lifecycle, external target validation, Caddy state, and retryable application results. | Domain/TLS/Caddy behavior remains in the module; provider and certificate regression evidence is open. |
| D13 | Website health checks, automatic monitoring controls, operational health alerts, backups, restores, verification, and log snapshots | `BackupController`, website health routes/models, backup/health/log jobs and commands | `app/Modules/Deployer/Websites` and `Backups` | Preserve backup destinations/credentials, retention, integrity evidence, restore safety, health thresholds, and private log access. | Website health and backup behavior remains in the module; restore, integrity, retention, and alert evidence is open. |
| D14 | Database discovery/inspection, snapshot/backup collection, cloning, database users, expiration and managed backup verification | `DatabaseController`, database jobs/commands, database operation and managed backup models/migrations | `app/Modules/Deployer/Databases` and `Backups` | Preserve credentials, access boundaries, snapshot provenance, destructive-operation safeguards, and retry/audit history. | Database operation behavior remains in the module; provider, backup, clone, and destructive-action evidence is open. |
| D15 | Provider, server, website, database, and infrastructure cost views, budgets, and reporting | `CostController`, cost models/queries, budget routes | `app/Modules/Deployer/Costs` | Preserve provider-specific cost sources, currency/time windows, workspace visibility, and budget thresholds. | Cost and budget behavior remains in the module; calculation/source comparison evidence is open. |
| D16 | Load balancers, node membership/health, apply/remove operations, and scheduled scaling | `LoadBalancerController`, `ApplyLoadBalancerJob`, `RemoveLoadBalancerJob`, `CheckLoadBalancerNodesCommand`, scaling schedules | `app/Modules/Deployer/LoadBalancers` | Preserve provider credentials, node membership, health checks, scheduled action authorization, and reconciliation safety. | Load-balancer and scaling behavior remains in the module; provider, schedule, and reconciliation evidence is open. |
| D17 | Deployment/scaling schedules, scheduled tasks, automation workflows, and environment hibernation/wake behavior | `AutomationController`, schedules/jobs/commands, `EvaluateEnvironmentHibernationJob`, `WakeHibernatedEnvironmentJob` | `app/Modules/Deployer/Automation` | Preserve timezone and recurrence semantics, ownership, run history, wake safeguards, and idempotent queued work. | Automation and environment lifecycle behavior remains in the module; schedule and state-transition evidence is open. |
| D18 | Recipe management, gallery install/refresh, favorites, ratings, abuse reports/review, and revision history | `RecipesController`, `RecipeGalleryController`, `RecipeFavoritesController`, `RecipeRatingsController`, `RecipeReportsController`, gallery notifications | `app/Modules/Deployer/Recipes` | Preserve public/private visibility, script encryption, revision attribution, moderation roles, and installation safety. | Recipe/gallery behavior remains in the module; publish/install/moderation regression evidence is open. |
| D19 | Observability views, environment context, saved investigations, operational incident findings/export, and release observation | `ObservabilityController`, `OperationalIncidentController`, deployment observation jobs/commands and models | `app/Modules/Deployer/Observability` | Preserve retention, environment scoping, incident evidence, report access, and correlation to deployments/builds. | Observability and incident behavior remains in the module; investigation/export/correlation evidence is open. |
| D20 | Platform health/reporting and public status pages, public JSON reports, subscriptions, confirmations, and unsubscribe | `SystemHealthController`, `PlatformStatusController`, `PublicStatusPageController`, `StatusSubscriptionController`, status notifications | Core platform health plus Deployer status module | Preserve unauthenticated response boundaries, notification consent/tokens, rate limits, status page history, and health semantics. | Public health/status behavior remains in the module; unauthenticated route and subscriber workflow evidence is open. |
| D21 | Activity feed/export, notifications/inbox/read state/saved filters/export, search, and dashboard preferences | `ActivityController`, `NotificationsController`, `SearchController`, `DashboardController`, notification models | Core inbox/search shell with module-authoritative data | Preserve per-user/workspace visibility, unread state, filters, export scopes, search permissions, and saved preferences. | Activity, notification, search, and dashboard behavior remains in the module; filtering/export/access evidence is open. |
| D22 | Public API/control plane, API docs/OpenAPI, health check, signed callbacks, repository/GitHub webhooks, alert webhooks, and scoped API tokens | Source `routes/api.php`, callbacks, `Api/V1/ControlPlaneController`, webhook controllers and token models | Deployer API with Core auth/token identity | Keep paths, payloads, API versions, tokens/abilities, signatures, throttles, idempotency, callback access, and public ingest independent of browser sessions. | Deployer API routes and callbacks remain in the module; API compatibility, token, signature, and retry evidence is open. |
| D23 | Account export/deletion, profile/security/social identity controls, sign-in history/export/pruning, access requests, administrator access-review/analytics, feedback, email diagnostics, and email readiness | `AccountDataController`, `AccountDeletionController`, `SignInHistoryController`, admin controllers, feedback/email controllers, relevant notifications and pruning commands | Core account/security/admin plus Deployer support operations | Preserve legal export/deletion semantics, sensitive-action confirmation, least-privilege admin access, privacy/redaction, and audit history. | Account and admin behavior remains in the module; account lifecycle, privacy, and admin authorization evidence is open. |
| D24 | Public marketing, pricing, access request, legal/docs/API docs, and service health pages | `PublicPageController`, `AccessRequestController`, public routes and views | Core public site plus Deployer docs/pricing | Keep existing customer entry points, access request throttles, product pricing/plan copy, public API schema, and legal links. | Core `/` overview and `/deployer`, `/monitor`, `/analytics` product descriptions use the shared Signal public layout; focused tests and live HTTP checks pass. Product-subdomain roots lead guests to central auth. Pricing, access-request, legal/docs/API, status-page inventory, and complete content-parity checks remain open. |
| D25 | Scheduled maintenance, retention/pruning, queue execution, retries, alerts, and operational reporting behind Deployer workflows | `app/Console/Commands/*`, `app/Jobs/*`, `app/Notifications/*`, scheduler and queue config | `app/Modules/Deployer` workers/scheduler | Preserve schedules, worker queues, retries, lock/idempotency behavior, monitoring heartbeat, pruning windows, and notification triggers. | Deployer commands/jobs remain in the module; scheduler, worker, retry, and retention evidence is open. |

## Monitor

Monitor owns a separate database and product plan. Its `telemetry`, `checks`, and `alerts` database queues are bound explicitly to the `monitor` connection, and each queue guard checks the transaction level on that same connection. `QueueDatabaseIsolationTest` verifies that an open Deployer transaction cannot satisfy a Monitor queue guard. Durable write/worker acceptance across the production database engines remains open.

| ID | Capability to preserve | Source evidence to audit/migrate | Unified destination | Data, access, and integration constraints | Status / regression evidence |
| --- | --- | --- | --- | --- | --- |
| M01 | Registration, sign-in/out, email verification, password reset, workspace switching, invitations, member roles, and membership management | `Auth/*Controller`, `WorkspaceController`, `WorkspaceInvitationController`, `WorkspaceMemberController`, workspace routes | Core identity/workspaces plus Monitor access projection | Migrate identities and invitations; preserve workspace roles, verification, ownership, and local IDs through explicit maps. | The production identity import added the eligible Monitor account and kept the conflicting account under review. The workspace importer mapped one of two workspaces and the eligible membership; Core invitations and role management are implemented. Monitor invitation reconciliation, the held owner/workspace, product-principal provisioning, workspace CRUD, role changes/removal, and full authorization regression remain open. |
| M02 | Application and environment create/update/delete/restore, API keys, and environment-scoped configuration | `ApplicationController`, `EnvironmentController`, `EnvironmentIngestionController`, workspace/app/environment models | `app/Modules/Monitor/Applications` with Core project/environment links | Map each Monitor application to a distinct Core project and its environments to logical project environments; preserve source IDs, soft deletion, pause state, telemetry counters, and keep ingest credentials in Monitor. | The production product-database merge preserved Monitor-local records. Core mapped one workspace, three applications, and four environments; one workspace and its application remain held because their owner identity is unresolved. The importer retains source IDs, settings, environment state, counters, timestamps, and deletion state. Customer-data reconciliation, Monitor CRUD, credential lifecycle, restore behavior, and cross-app environment context regression remain open. |
| M03 | Deployment ingest and deployment history with release/build context | `DeploymentIngestController`, `DeploymentController`, `ReleaseController`, ingest token middleware | `app/Modules/Monitor/Deployments` and integration contract | Preserve ingest auth, idempotency, payload compatibility, timestamps, source identifiers, and Deployer linkage as an optional explicit connection. | The unified `/api/v1/deployments` route now has an endpoint contract regression covering required bearer-token auth, token-derived environment scope, response/cache headers, stored release identity, and idempotent replay without duplicate deployment/release rows. Cross-product linkage, field-validation compatibility across the source payload inventory, and broader deployment-history role coverage remain open. |
| M04 | Event ingestion, identity and batch handling, receipts, replay lookup, and rate-limited public ingest | `IngestController`, `IngestReceiptController`, telemetry ingest models/migrations | `app/Modules/Monitor/Ingestion` | Keep public machine access separate from browser auth, preserve receipts/idempotency, schema versions, tenant quotas, redaction, and durable processing. | The unified `/api/v1/ingest` contract verifies bearer-token access, queued acceptance, encrypted payload retention, receipt lookup, and idempotent batch replay; the regression also verifies accepted events are not written before the worker processes them. Rate-limit boundaries, worker failure/recovery, usage accounting, redaction coverage, and representative source-client payloads remain open. |
| M05 | OpenTelemetry traces/logs/metrics ingest and API documentation/OpenAPI | `OtlpIngestController`, `ApiDocumentationController`, `/v1/otlp/*`, `/v1/openapi.json` | `app/Modules/Monitor/Ingestion` and developer docs | Maintain OTLP endpoint semantics, auth token scopes, signal selection, throttles, payload handling, and generated API contract. | The unified OTLP route regression verifies an authenticated empty traces export returns the standard successful response and does not write telemetry or update collection counters. Non-empty traces/logs/metrics fixtures, OTLP receiver/client interoperability, and full generated-contract comparison remain open. |
| M06 | Queue snapshots, worker status, queue keys, heartbeat checks, heartbeat tokens, and health ingest | `QueueSnapshotController`, `QueueWorkerController`, `HeartbeatController`, token controllers and middleware | `app/Modules/Monitor/QueueHealth` | Preserve secret rotation/revocation, per-queue limits, stale worker detection, and transactionally consistent queue/monitor records. | Not migrated; token and transactional queue evidence open. |
| M07 | Main and saved dashboards, widget layout/configuration, dashboard access, and overview summaries | `DashboardController`, `SavedDashboardController`, `Dashboard`, `DashboardWidget` | `app/Modules/Monitor/Dashboards` | Preserve dashboard visibility, widget configuration, workspace ownership, and saved views. | Not migrated; dashboard render/access evidence open. |
| M08 | Event list/detail/search, metric series list/detail, dependency map, and data freshness | `EventController`, `MetricSeriesController`, `DependencyMapController`, event/metric models | `app/Modules/Monitor/Telemetry` | Preserve filters, pagination, aggregation/window semantics, environment boundaries, freshness indicators, and retention. | Not migrated; query/result comparison evidence open. |
| M09 | Trace and trace-event inspection, issue grouping/details/state/activity, issue digests, and notification preferences | `TraceController`, `TraceEventController`, `IssueController`, issue/digest models and notifications | `app/Modules/Monitor/Issues` and `Telemetry` | Preserve trace correlation, grouping/fingerprints, issue state history, email digest preferences, and restricted event payloads. | Not migrated; grouping/state/digest evidence open. |
| M10 | Release/deployment views and deployment-to-error context | `ReleaseController`, `DeploymentController`, release/deployment models and ingest | `app/Modules/Monitor/Deployments` | Preserve release identity, configured context windows, per-workspace plan limits, and optional cross-product linkage. | Not migrated; linking/window comparison evidence open. |
| M11 | Alert rules, filters, routing, destinations, escalation policies, delivery attempts, testing, retries, and secret rotation | `AlertRuleController`, `AlertRoutingController`, `AlertEscalationController`, destination/delivery controllers, jobs/models | `app/Modules/Monitor/Alerts` | Preserve rule semantics, signed webhooks, encrypted destination secrets, escalation timing, attempt history, access control, and safe retry. | Not migrated; route/delivery/retry evidence open. |
| M12 | Monitors/checks, scheduled check execution, queue-key and heartbeat-key lifecycle | `MonitorController`, monitor/check models, `checks` workers and token controllers | `app/Modules/Monitor/Checks` | Preserve cadence, retries, alert transitions, secrets and token scope, and durable check scheduling. | Not migrated; check/alert transition evidence open. |
| M13 | Incidents, incident activity, assignment/status updates, and incident notifications | `IncidentController`, `Incident`/`IncidentActivity`, `IncidentAlertNotification` | `app/Modules/Monitor/Incidents` | Preserve state changes, actor/timestamps, alert linkage, notification deduplication, and permission scope. | Open/acknowledged/resolved lifecycle changes now enter the Monitor outbox for connected Analytics timelines; the full Monitor lifecycle, notification parity, and role regression remain open. |
| M14 | Service-level objectives, burn-rate reporting/alerts, SLO reports and exports | `ServiceLevelObjectiveController`, SLO model, usage/alert jobs | `app/Modules/Monitor/Objectives` | Keep SLO calculation windows, event source/retention coverage, plan entitlements, exports, and alert thresholds. | Not migrated; calculation/report evidence open. |
| M15 | Maintenance windows and suppression behavior | `MaintenanceWindowController`, `MaintenanceWindow` model | `app/Modules/Monitor/Maintenance` | Preserve timezone/schedule, affected monitor/rule scope, state, and suppression semantics without dropping incoming telemetry. | Not migrated; scheduling/suppression evidence open. |
| M16 | Public status pages, component status, reports, subscription, confirmation, and unsubscribe | `PublicStatusPageController`, `StatusPageController`, status models and notification flows | `app/Modules/Monitor/StatusPages` | Keep public pages unauthenticated, status history accurate, subscriber consent/tokens safe, and workspace access private. | Not migrated; public behavior and subscription evidence open. |
| M17 | Workspace product plans, Stripe checkout/portal, webhook reconciliation, event usage, limits, and retention by tier | `BillingController`, `StripeWebhookController`, `BillingEvent`, `TelemetryUsageEntry`, `config/beacon.php` | `app/Modules/Monitor/Billing` | Maintain an independent Monitor subscription and price/limit catalog; verify billing event idempotency, usage period boundaries, retention per tier, and no cross-product entitlement changes. | The production Core import reconciled the current Monitor plan snapshot for one eligible workspace; one workspace remains held for identity/resource review. No Stripe customer/subscription or billing-event record was imported in this pass, and the source contains no billing-event history. Normalized Core entitlements/limits and the opt-in `MONITOR_PLAN_AUTHORITY=core` adapter are implemented; default authority remains `legacy`. Billing UI/POST endpoints stay read-only in Core mode until Stripe lifecycle integration is complete. Checkout/portal/webhook integration, ownership reconciliation, and full Monitor feature regression remain open. |
| M18 | Slack, Microsoft Teams, PagerDuty, and signed webhook destinations plus integration diagnostics | `IntegrationController`, destination adapters, notification/delivery jobs and config | `app/Modules/Monitor/Integrations` | Preserve connection credentials, delivery format/signatures, provider failure handling, and explicit project/workspace authorization. | Not migrated; provider contract evidence open. |
| M19 | Audit log, workspace data export, notification settings, team/billing/data/API settings | `AuditLogController`, `WorkspaceDataController`, `NotificationPreferenceController`, settings routes | Core workspace settings plus `app/Modules/Monitor` settings | Preserve actor/target audit history, export scopes, notification choices, and sensitive billing/data access. | Not migrated; export/audit/privacy evidence open. |
| M20 | Telemetry/check/alert async workers, issue digest, usage alerts, pruning/retention, database schema maintenance, and failure recovery | `config/queue.php`, `app/Modules/Monitor/Services/*Queue.php`, Monitor migrations | `app/Modules/Monitor` queues/scheduler | Keep domain writes and database queue insertion on the same Monitor transaction; preserve queue names, worker concurrency, retries, locks, pruning/retention, and alert delivery. | The three Monitor queue connections point at the Monitor database; dispatch guards inspect its explicit transaction level. `QueueDatabaseIsolationTest` confirms an unrelated Deployer transaction is rejected. Transactional enqueue/recovery worker acceptance against each production database engine remains open. |
| M21 | Authentication middleware, CSRF/CORS/public endpoint boundaries, rate limits, token scopes, and input redaction | `app/Http/Middleware/*`, route middleware, `config/beacon.php`, throttle definitions | Core request security plus Monitor middleware/policies | Public ingestion must remain machine-authenticated and throttled; browser auth must not replace tokens; redact sensitive telemetry and enforce tenant policies. | Monitor bearer-token guards, request decoding/size controls, and route throttles are present in the module; browser routes select Core authentication in production. Ingest/deployment/OTLP contract tests cover token-required acceptance and selected replay boundaries. Complete CSRF/CORS review, every token scope and throttle, redaction coverage, role authorization, and threat regression remain open. |

## Analytics

Analytics currently has workspace/site ownership, local identity records and no subscription implementation. Add an independent Analytics product plan at workspace level as an approved new requirement; do not pretend that an existing Analytics subscription is being migrated.

| ID | Capability to preserve | Source evidence to audit/migrate | Unified destination | Data, access, and integration constraints | Status / regression evidence |
| --- | --- | --- | --- | --- | --- |
| A01 | Registration/sign-in, verification, account profile, passkeys, and two-factor behavior configured through Fortify | Fortify configuration/provider, `ProfileController`, `User`, auth routes and auth-related migrations | Core identity/security | Preserve supported credentials, verification/security state and account settings; consolidate credentials only through explicit identity mapping. | The production account mapping preserves eligible Analytics credentials and verification state without email merging. The account import is reconciled, but Analytics auth providers/routes remain inactive; apply rechecks earlier review mappings after source keys/conflicts are corrected. Core security and account-flow regression evidence remains open. |
| A02 | Workspace creation/selection, team roles, invitations, acceptance, and member removal/role updates | `WorkspaceController`, `TeamController`, `InvitationController`, `Workspace`, `Invitation` | Core workspaces/memberships | Preserve workspace ownership, roles, pending invitation tokens/expiry, and invitation acceptance; keep Analytics-local access rules. | The production importer mapped both Analytics workspaces and their eligible memberships into Core. Core provides workspace invitations and membership management; Analytics invitation reconciliation, workspace CRUD, role updates/removal, product-principal provisioning, and product-local authorization regression remain open. |
| A03 | Site onboarding, public ID generation, setup, verification, settings, and site deletion | `SiteController`, `SiteSettingsController`, `Site`, site routes | `app/Modules/Analytics/Sites` with canonical project link | Map site to project explicitly; preserve public IDs, verified domains, settings, consent/privacy configuration, and deletion semantics. | The production importer preserved and mapped the existing Analytics site, public ID, domains, excluded paths, timezone, verification/collection state, and archived status. Live site CRUD, consent/privacy parity, project-context navigation, and report regression remain open. |
| A04 | Browser tracker asset and public event collection/preflight endpoints | `/tracker/v1.js`, `CollectEventsController`, `/api/v1/collect/{publicId}`, public ID validation | `app/Modules/Analytics/Collection` | Preserve collector path and payload/CORS behavior, public ID validation, rate limits, event consent behavior, tracker compatibility, and `ANALYTICS_VISITOR_KEY` semantics. | The existing asset and endpoint remain at their established paths on the Analytics module/database. The tracker now sends RFC 4122 event, site-scoped visitor, and session IDs; honors consent and opt-out before storage or network use; tracks SPA history changes; and retries transient delivery failures. Browser coverage exercises those behaviors and the legacy-format UUID fallback; endpoint tests cover origin rejection, CORS preflight, payload privacy, and server-side visitor hashing. Deployed-host CORS/tracker smoke checks and rate-limit/load acceptance remain open. |
| A05 | Event batches, visitor identities, visits, deduplication, ingestion processing, and daily aggregates | `AnalyticsEvent`, `IngestionBatch`, `Visit`, `ReportDailyAggregate`, `ProcessEventBatch`, dispatch/prune commands | `app/Modules/Analytics/Ingestion` and reporting | Preserve event schema/timestamps, visitor identity, batching order/retries, aggregate correctness, workspace/site isolation, and retention. | Module behavior is present and Analytics regression tests cover event intake, duplicate-ID handling, asynchronous processing, visits, goals, reporting aggregates, and retention. `AcceptEventBatch` now counts only inserted events and drops empty duplicate-only batches. Representative historical-data comparison and live worker failure/retry acceptance remain open. |
| A06 | Analytics overview/dashboard and site traffic/report views | `App\Livewire\Dashboard\Overview`, reporting queries/aggregates, dashboard views | `app/Modules/Analytics/Dashboard` | Preserve date/timezone filters, visitor/session/event calculations, empty/stale states, and report access rules. | The Analytics Livewire overview and report query remain available under the module routes, including site/timezone context, period comparisons, path/source/campaign/device filters, daily-aggregate long ranges, traffic breakdowns, goal summaries, and release/incident annotations. Report controls and cards use the shared Signal components; breakdown links preserve selected site, date range, and filters. Module regressions cover the rendered overview, multi-filter metric/breakdown scope and site isolation, and long-range aggregate use. Representative source-data report comparison, role/host rehearsal, and customer-data rehearsal remain open. |
| A07 | Goals, goal versions, conversion tracking and conversion reporting | `GoalController`, `Goal`, `GoalVersion`, `GoalConversion` | `app/Modules/Analytics/Goals` | Preserve goal definitions/version history and conversion attribution; ensure existing event history continues to calculate under migrated identifiers. | Analytics goal create/edit/delete screens remain available in the module and now use shared Signal field, input, select, and checkbox components. The checkbox submits explicit active/paused state; updates preserve matching-rule versions and rebuild visits, conversions, and aggregates. Regression verifies pause/resume, version history, deletion, and rejection of a cross-site goal edit. Historical report comparison, Core-authenticated host rehearsal, and customer-data rehearsal remain open. |
| A08 | Asynchronous report export creation, status, authorized download, and failure recovery | `ReportExportController`, `ReportExport`, `GenerateReportExport` | `app/Modules/Analytics/Exports` | Preserve export scope, expiry/token behavior, background retries, private file storage and download authorization. | The token-hash request/status/download flow and Analytics-queue CSV job are implemented. Regression covers an authorized request through generation and download, plus a storage-write failure remaining failed instead of being reported as complete. Expiry/retry recovery, role matrix, source-data comparison, and customer-data rehearsal remain open. |
| A09 | Readiness/health, Horizon queue dashboard, pending-batch dispatch, data pruning/retention, and owner provisioning operations | `ReadinessController`, `HorizonServiceProvider`, Horizon config, `DispatchPendingBatches`, `PruneAnalyticsData`, `ProvisionAnalyticsOwner`, `routes/console.php` | Core health plus Analytics workers/scheduler | Preserve service-readiness semantics, queue dashboard authorization/host scope, worker schedules, retention windows, idempotent provisioning and operational alerts. | Horizon is opt-in through `ANALYTICS_HORIZON_ENABLED` and requires `ANALYTICS_QUEUE_DRIVER=redis`; it binds to the exact Analytics host, uses an Analytics-specific Redis connection/prefix/queue, and authorizes only active Core platform admins in the configured allowlist. Disabled-by-default and authorized-route registration checks plus the access policy test pass. Worker sizing, deployment/runbook, queue monitoring/alerts, and production acceptance remain open. |
| A10 | Workspace invitation notifications, analytics-specific access controls, and privacy/data settings | `WorkspaceInvitation` notification, workspace/site policies and relevant configuration | Core notifications/access plus `app/Modules/Analytics` policies | Preserve notification content/deduplication, site privacy rules, exports/deletion requirements, and product-local authorization. | Core's workspace invitation notification and token-bound acceptance screen are implemented with Signal components. Analytics-specific notification preference parity, site access/privacy rules, exports/deletion requirements, and regression evidence remain open. |
| A11 | Independent Analytics subscription and usage entitlement lifecycle | No current billing controller/subscription model found in audited Analytics revision | `app/Modules/Analytics/Billing` with Core workspace plan authority | Define Analytics plans and usage limits as a separate workspace subscription; never couple it to Deployer or Monitor billing. | Analytics has no source billing catalog, so the production import initialized a separate $0 `legacy_access` Core slot for each of its two reconciled workspaces. Existing features and configured retention windows are preserved without Stripe or invoice records. `ANALYTICS_PLAN_AUTHORITY=core` reads the immutable Core snapshot; default remains `legacy`. Paid catalog, usage limits, billing UI/checkout/portal/webhooks, and commercial acceptance criteria remain open. |

## Shared platform Core

| ID | Capability to preserve | Source evidence to audit/migrate | Unified destination | Data, access, and integration constraints | Status / regression evidence |
| --- | --- | --- | --- | --- | --- |
| C01 | Workspace overview, project directory, per-product subscriptions/access, team summary, and recent app connections | `CoreHomeController`, `WorkspaceDashboardController`, `WorkspaceProjectsController`, shared Signal layout/components | `app/Core/Workspaces` with `x-signal.layouts.platform` | Scope projects and connections by active workspace/project membership and product grants; expose billing terms only to billing managers; keep module summaries isolated by product connection. | Signal Topbar SaaS workspace overview shows separate plan cards, authorized recent connections, and bounded latest-deployment, Monitor health/incident, and Analytics traffic snapshots for up to six pinned and six recently updated projects. Summaries require an active Core product link and current member grant, then each module rechecks source-resource access; freshness, state, and product links are shown, and module outages degrade per product. A ranked priority panel surfaces eligible connection failures, product-reported deployment/health issues, and one unfinished setup action for those projects. Core now persists personal/workspace project pins, named product/pinned-only/project-name views, authorized canonical-environment filters, and each user's selected view per workspace; project-name matching is literal and SQL wildcard characters are escaped. Shared preferences require owner/admin authority; view data is filtered again by active membership, project membership, and current app grant on every load, with revoked product filters shown as unavailable. Operational-state filters, workspace-wide rollups, billing management, and account/security remain open; product-native dashboards stay in place. Workspace overview links to a dedicated activity page for up to 30 recent authorized connected-app workflow runs, with project links and owner/admin single-step retry; module-native deployments and exports still need task-provider adapters. |
| C02 | Workspace team, member roles, invitations, separate app access, product seats, revocation, and access history | `WorkspaceTeamController`, `WorkspaceInvitation` flows, per-product plan resolvers, local workspace membership models | Core workspace memberships and product grants projected into `app/Modules/{Deployer,Monitor,Analytics}` | Keep app grants and seat limits independent, preserve local role semantics and ownership, require explicit identity/workspace maps, and audit all changes. | The Signal team page now assigns separate product access and checks each app's active Core plan/seat cap. Grants project least-privilege roles into explicitly mapped app workspaces, where Deployer `member` becomes `developer` and Analytics `member` becomes `viewer`; product admin grants are owner-managed. Revocation blocks Core-authenticated access and offers a manual retry if local cleanup could not reach a product database. A five-minute background retry handles explicitly tracked cleanup after exact identity mappings and product adapters become available; ambiguous mappings remain pending. Imported product-owner roles are shown with a clear revoke action and cannot be silently demoted. Seventeen focused tests with 153 assertions cover preview/no mutation, cleanup after reconciliation, idempotency, shared-member removal, independent grants, seat exhaustion, role mapping, audit history, ownership preservation, and Deployer request-local workspace context. Full product capability matrix, invitation/source-role reconciliation, and route/action/job regression remain open. |

## Cross-product checks before production cutover

| Check | Required evidence | Current status |
| --- | --- | --- |
| Identity and workspace reconciliation | Stable source-to-Core user/workspace maps; duplicate/conflicting identities reviewed; roles and pending invitations reconciled; credential and MFA import rehearsed. | Partially reconciled in production: eligible Deployer accounts/workspaces and the eligible Analytics account/workspaces are mapped; one Monitor account/workspace collision remains held, and Deployer paid billing ownership across two organizations remains unresolved. Roles, invitations, source-qualified identity maps, credential and MFA checks, and complete owner review remain open. |
| Project and resource mappings | Explicit Core project mappings for Deployer projects, Monitor applications, and Analytics sites; environment mappings reviewed by workspace owners; no name-based automatic linking. | Production importers mapped Deployer 3 workspaces/5 projects/9 environments; Analytics 2 workspaces/1 site; and the eligible Monitor workspace with 3 applications/4 environments. The conflicting Monitor workspace and one application remain held. Database integrity/foreign-key checks passed; workspace-owner confirmation, access regression, action/job scoping, cross-host browser acceptance, delta sync, and cutover rehearsal remain open. |
| Database and migration isolation | Each product migration/model/job/query uses its named product connection; Core data stays in Core; no cross-database joins or default-connection dependence; rollback and backup evidence recorded. | Named connections exist and Core, Monitor, and Analytics models use them. Deployer still has established raw DB/Schema and migration paths that rely on the stable `deployer` default, so Core cannot become the global default yet. The generic database/failed-job queues now explicitly use Deployer; Monitor queue guards reject unrelated Deployer transactions. Full migration/query audit and four-database production-engine evidence remain open. |
| Product subscription separation | Separate workspace plans, billing history, usage, quotas, cancellation, and entitlements for Deployer, Monitor, and Analytics; verify one product's state cannot mutate another's. | Partially reconciled: Core has one Deployer free slot, one Monitor current-plan slot, and two Analytics no-charge legacy slots. The Deployer paid owner subscription is still retained in its source database because it is shared across two organizations; Monitor has one held workspace and no source billing-event history. Billing lifecycle, usage, seats, provider webhooks, independent-change tests, and ownership confirmation remain open. |
| Inter-app connections | Explicit, revocable project/environment mapping and capability grants; authorized versioned events; idempotency, failure state and audit history; no implicit deployment or subscription change. | Deployer → Monitor deployment context and Deployer → Analytics release annotations use a transactional Deployer outbox; Monitor → Analytics incident state annotations (open, acknowledged, resolved) use a transactional Monitor outbox. The Analytics → Monitor connection is now an active read-only mapping; Monitor incident investigations display plan-bounded pageview and distinct-visitor aggregates for the incident window and preceding comparison window. All paths recheck applicable grants/plans and connection state; replay/reconciliation, cross-database failure rehearsal, and full access/production acceptance remain open. |
| API, webhook, tracker and callback compatibility | Contract fixtures and representative real-client requests for every existing public endpoint; signatures, auth, rate limits, CORS, versions, and retry semantics retained. | Open. |
| Queues and scheduled work | Inventory all commands/jobs/schedules and operational runbooks; preserve product-specific workers, queue durability, transactional Monitor enqueues, retry/lock semantics, and retention. | Deployer, Monitor, and Analytics queue connections are named; Monitor tests protect its transaction boundary when Deployer is the default. Worker/scheduler inventory, retry/retention regression, and production-engine acceptance remain open. |
| Feature parity and access control | Every row above mapped to a concrete route/screen/job/API and validated for each role, product plan, and workspace; inaccessible features remain unauthorized after navigation consolidation. | Workspace access on Core-authenticated Deployer, Monitor, and Analytics paths checks the selected source workspace mapping, active Core membership, and active product grant; per-app grants and seat caps are managed independently from the shared team page. Full route/policy matrix, feature-specific entitlement enforcement, product onboarding, complete role-capability mapping, and cross-host access regression remain open. |
| Data migration and cutover | Source backups, dry-run imports, row/count reconciliation, encrypted/sensitive-field checks, repeatable delta sync, customer communications, DNS/rollback rehearsal and owner sign-off. | Initial production imports are complete with private backups and verified SQLite integrity/foreign keys. Reconciliation still has an unresolved Monitor identity/resource collision and Deployer shared-owner billing decision. Delta sync, encrypted-secret and representative-history comparison, restore drill, production-engine validation, traffic/DNS handoff, rollback, and owner sign-off remain open. |
