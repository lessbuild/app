# Buildpusher marketing pages progress — 2026-09-25

The Core public site has a Signal-styled suite page at `/` and product pages at `/deployer`, `/monitor`, and `/analytics`. They use the shared Signal public document and reusable blocks, with the configured product subdomains as their app actions.

The suite page explains the three real product roles and their shared-project delivery loop: Deployer release controls, Monitor telemetry and incident response, and Analytics reports with Deployer release and Monitor incident annotations. It states that annotations depend on explicit resource mapping to a shared project and that each product retains its own operational records and access rules. It does not imply that Analytics' still-pending paid billing lifecycle is available. A Signal-style product explorer now lets visitors switch between Deploy, Monitor, and Analytics, compare each app's workflow and implemented capabilities, and open the matching detail page. The explorer degrades to in-page links and visible panels without JavaScript; when enabled, its tabs support arrow, Home, and End keyboard navigation.

The product catalog supplies each page's highlights, detailed capability groups, workflows, security controls, FAQs, and illustrative interface. Monitor's catalog calls out its HTTP, DNS, TLS, TCP, queue/worker, and cron/heartbeat checks. Analytics' catalog includes the shipped page, source, campaign, entry/exit, device, browser/OS, path-goal, event-goal, recent-traffic, release-annotation, and incident-annotation capabilities. The previews clearly identify all displayed values and activity as fictional sample data.

The product pages now have an "At a glance" Signal badge strip backed by each product's real capabilities: Deployer's DigitalOcean, Hetzner Cloud, Vultr, Cloudflare DNS, GitHub, GitLab, and Bitbucket connections; Monitor's OpenTelemetry intake, Deployer release context, and email, signed webhook, Slack, Microsoft Teams, PagerDuty, and Discord alert destinations; and Analytics' cookieless estimates, UTM attribution, goals, audience reports, filtered CSV exports, and mapped release/incident context. Monitor's copy states that Deployer release context depends on connecting the resources to the same Core project.

The capability strip now appears directly below each product hero so visitors can scan real integrations and features before the deeper catalog. Highlight grids adapt to each app's three or four highlights, and the suite's shared-project section directs signed-in users to their workspace and new visitors to workspace creation.

The suite and relevant product pages now also explain the four shipped cross-product workflows: Deployer release context in Monitor, Deployer release annotations in Analytics, Monitor incident lifecycle annotations in Analytics, and read-only Analytics pageview/visitor context in Monitor incident investigations. Reusable Signal connection cards state the source app, destination app, and workflow type. The pages make clear that these connections require explicit resources on a shared project and current product access/entitlements; product-owned records remain in their originating databases.

`MarketingPagesTest` and `SignalThemeArchitectureTest` now cover the explorer's server-rendered content and conditional Signal asset loading. A focused Playwright test covers pointer and keyboard tab interaction. These tests were **not run**, following the instruction to defer tests until the full implementation plan is complete.

For the cross-product workflow cards, PHP syntax checks, Pint, the marketing route listing, Blade view caching, and `git diff --check` passed. No PHPUnit, browser, or other tests were run.

Static verification for this slice passed: `node --check resources/js/signal-marketing.js`, `npm run build`, `php artisan view:cache` (then cleared), route listing, and `git diff --check`. No test suite was run. These page changes do not close full marketing-content parity, pricing/access-request/legal/help/API/status inventory, authenticated cross-host acceptance, or the remaining plan milestones. No production release was made.

The suite landing page now describes the shared Core features teams use around the product dashboards: canonical project links, independent product access, authorized activity and notifications, product plan-status review, Deployer cost breakdowns, customer status pages, help/API docs, and feedback. A new reusable Signal workspace-capability block keeps those cards themeable. The suite FAQ explains that plan/access state is separate per app while avoiding unsupported pricing or billing-lifecycle promises. The suite and product footers link directly to Core Help/API docs and platform status. Regression assertions were added but remain unrun until the plan is complete.

The latest content pass adds more verified app workflows to the product catalogs and suite cards: Deployer configuration plans, build comparison and promotion, and protected-secret controls for preview environments; Monitor saved dashboard layouts, maintenance context, and issue digests; Analytics' consent-aware browser tracker, single-page navigation support, and timezone-based report comparisons. The marketing regression assertions now cover those additions and render every suite-workflow feature from the shared catalog. They remain unrun per the test deferral instruction. PHP syntax, Pint, route listing, Blade view caching, and `git diff --check` passed for this pass. No production release was made.

The 2026-09-25 check of the upstream Signal repository confirmed that `main` still resolves to `794d273ebd4635ff124981f0fcddce89e9d7c2b1`. The suite hero now adapts Signal's current shared-project dashboard preview, showing a sample Deployer release, Monitor service health, and Analytics traffic estimate; the project-to-app map remains in the shared-project section. The Analytics product preview follows Signal's progressive 7-, 30-, and 90-day chart selector using shared Signal controls. Those values and charts remain labeled fictional sample data. Marketing feature tests and the browser interaction regression were updated but **not run**, following the test deferral instruction. No production release was made.
